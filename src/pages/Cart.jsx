import React from "react";
import CartItems from "../components/CartItems";
import "../styles/Cart.css";

const Cart = () => {
  return (
    <section>
      <CartItems />
    </section>
  );
};

export default Cart;
